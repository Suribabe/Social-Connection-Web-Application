from django.conf.urls import include, url

import django.contrib.auth.views
import grumblr.views

urlpatterns = [
    # url(r'^$', grumblr.views.home),
    # # url(r'^register',grumblr.views.register),
    # url(r'^main',grumblr.views.home),
]
